---
# JMX connection settings
hostPort: localhost:5555
username:
password:

# Optional: Custom metrics endpoint path (defaults to /metrics if not specified)
# httpServer:
#   path: /metrics

# Metric collection rules
rules:
  - pattern: ".*"
